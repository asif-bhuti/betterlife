Direct into the folder named "betterlife" and then type: 

npm start

--------
if warnings, errors or/ and issues arises then:

npm install styled-components
npm install react-router-dom
npm install react-icons
npm install axios
-----------------------
after all the installation is done, typ:

npm start
